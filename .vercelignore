# Vercel Ignore - Frontend Only Deployment
# Backend runs entirely on AWS Lambda

# Local development backend (not needed in production)
app/api/
app/lib/database.ts
app/lib/auth.ts

# AWS backend (deployed separately via SAM)
aws/

# Local development data
data/

# Docker files (not used)
Dockerfile
docker-compose.yml

# Environment files
.env.local
.env.example

# SAM configuration
samconfig.toml
